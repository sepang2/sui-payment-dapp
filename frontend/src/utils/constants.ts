// 결제 관련 상수
export const DISCOUNT_RATE = 0.05; // % 할인율
export const EXCHANGE_RATE = 3500; // SUI to KRW 환율

// 사용자 타입 enum (클라이언트 사이드용)
export enum UserType {
  STORE = "STORE",
  CONSUMER = "CONSUMER",
}
